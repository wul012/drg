// #pragma once
//
// #include <cstdint>
// #include <vector>
//
// /*
//  * =====================================================================================
//  *
//  *   文件名: nQueens.h
//  *
//  *   功能说明:
//  *       声明 "N 皇后问题"(包括经典的 8 皇后) 相关的接口函数。
//  *
//  *   问题描述:
//  *       在一個 N × N 的棋盘上摆放 N 个皇后(Queen)，要求任意两个皇后
//  *       之间都不能处于同一行、同一列、同一条斜线(包括左上-右下、左下-右上)。
//  *       问: 一共有多少种不同的摆放方式？
//  *
//  *   本文件提供两个求解接口:
//  *
//  *   1) countNQueensSimple(int n)
//  *      - 使用最直接的回溯(暴力尝试 + 检查合法性)方式求解，代码逻辑
//  *        与多数教科书中的实现类似，适合直观理解 N 皇后问题的约束条件。
//  *      - 时间复杂度大致为 O(N!)，当 N 稍大(> 13) 时会变得很慢，但实现简单。
//  *
//  *   2) countNQueensBitmask(int n)
//  *      - 使用位运算优化(与截图中的 Java 版本等价)，只用一个 32 位整型
//  *        同时表示哪些列/斜线已经不能再放皇后，极大提升了回溯的效率。
//  *      - 由于使用的是 32 位整型，本实现限定 1 <= n <= 32。
//  *      - 对于 n 取 8、10、15 甚至 20 以上仍然可以在极短时间内计算出结果。
//  *
//  *   两个接口都只返回 "解的个数"，不返回具体的棋盘摆放方案。
//  *   如果只想要 8 皇后的解个数，可以调用: countNQueensBitmask(8)。
//  *
//  * =====================================================================================
//  */
//
// namespace nQueens {
//
//     /**
//      * @brief 使用最朴素的回溯方法统计 N 皇后问题的解个数
//      *
//      * @param n 棋盘大小, 也是皇后数量 (N × N 棋盘上放 N 个皇后)
//      * @return  满足条件的所有不同摆放方式的个数
//      *
//      * @note
//      *   - 当 n <= 0 时返回 0；
//      *   - 时间复杂度极高，仅推荐在 n 不大的情况下使用(例如 n <= 12)。
//      */
//     long long countNQueensSimple(int n);
//
//     /**
//      * @brief 使用位运算优化的方式统计 N 皇后问题的解个数 (支持 1 <= n <= 32)
//      *
//      * @param n 棋盘大小, 也是皇后数量
//      * @return  满足条件的所有不同摆放方式的个数
//      *
//      * @note
//      *   - 如果 n 不在 [1, 32] 范围内，返回 0;
//      *   - 算法核心思想与截图中的 Java 版本完全一致: 通过三组 bitmask
//      *     (列限制、左斜线限制、右斜线限制) 来剪枝；
//      *   - 适合用来对比理解: 普通回溯 与 位运算优化 之间的性能差异。
//      */
//     long long countNQueensBitmask(int n);
//
// } // namespace nQueens

#include<cstdint>
#include<vector>
#pragma once
namespace nQueens {
    long long countNQueensSimple(int n);
    long long countNQueensBitmask(int n);
}